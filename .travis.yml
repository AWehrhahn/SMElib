language: c
compiler: gcc

jobs:
  include:
    - os: linux
      before_install:
        - sudo apt-get install gfortran
    - os: osx
      before_install:
        - brew reinstall libtool
        - brew install gcc
    - os: windows
      before_install:
        - choco install -y mingw

install:
  - ./bootstrap

script:
  - ./configure
  - make
