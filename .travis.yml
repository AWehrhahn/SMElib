language: c
compiler: gcc

jobs:
  include:
    - os: linux
      before_install:
        - sudo apt-get install gfortran
    - os: osx
      before_install:
        - brew reinstall libtool
        - brew install gfortran

install:
  - ./bootstrap

script:
  - ./configure
  - make
